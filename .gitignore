node_modules/
dist/
coverage/
playwright-report/
test-results/
*.local
.vercel
.env*
!.env.production
!.env.development
!.env.staging

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
